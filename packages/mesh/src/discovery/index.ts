/**
 * Unified discovery module — scanner, types, and exclude patterns.
 *
 * @module mesh/discovery
 */
export { unifiedScan } from './unified-scanner.js';
export type { ScanEvent, ScanProgress, UnifiedScanOptions } from './types.js';
export { UNIFIED_EXCLUDE_PATTERNS } from './types.js';

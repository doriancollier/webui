{
  "spec": "specs/upgrade-electron-vite/02-specification.md",
  "slug": "upgrade-electron-vite",
  "generatedAt": "2026-04-01T00:00:00Z",
  "mode": "full",
  "lastDecomposeDate": null,
  "tasks": [
    {
      "id": "1.1",
      "phase": 1,
      "phaseName": "Implementation",
      "subject": "[upgrade-electron-vite] [P1] Bump electron-vite, electron, and @tailwindcss/vite versions",
      "description": "Edit `apps/desktop/package.json` to bump three devDependencies. All other dependencies must remain unchanged.\n\nChanges to make in `devDependencies`:\n- `\"electron-vite\"`: `\"^3.1.0\"` → `\"^5.0.0\"`\n- `\"electron\"`: `\"^33.4.11\"` → `\"^39.8.5\"`\n- `\"@tailwindcss/vite\"`: `\"^4.1.18\"` → `\"^4.2.2\"`\n\nLeave these unchanged:\n- `\"@electron/rebuild\"`: `\"^3.7.2\"` (v4 has breaking API changes)\n- `\"@vitejs/plugin-react\"`: `\"5.1.4\"` (v6 requires Vite 8; electron-vite v5 bundles Vite 7)\n- `\"electron-builder\"`: `\"^26.8.1\"`\n- `\"electron-log\"`: `\"^5.4.3\"`\n- `\"electron-updater\"`: `\"^6.8.3\"`\n- `\"tailwindcss\"`: `\"^4.2.2\"` (already at target)\n- `\"typescript\"`: `\"^5.9.3\"`\n\nDo not modify `dependencies`, `scripts`, `main`, `name`, or `version` fields.",
      "activeForm": "Bumping package versions in apps/desktop/package.json",
      "size": "small",
      "priority": "high",
      "dependencies": [],
      "parallelWith": ["1.2"]
    },
    {
      "id": "1.2",
      "phase": 1,
      "phaseName": "Implementation",
      "subject": "[upgrade-electron-vite] [P1] Remove externalizeDepsPlugin from electron.vite.config.ts",
      "description": "Edit `apps/desktop/electron.vite.config.ts` to remove the deprecated `externalizeDepsPlugin`. In electron-vite v5, its behavior (treat all node_modules as external for main/preload) is now the default (`build.externalizeDeps: true` is on by default), so the explicit plugin call is no longer needed.\n\nThree changes required:\n\n1. **Import line** — remove `externalizeDepsPlugin` from the named imports:\n   - Before: `import { defineConfig, externalizeDepsPlugin } from 'electron-vite';`\n   - After:  `import { defineConfig } from 'electron-vite';`\n\n2. **`main` config block** — remove the `plugins` array entirely:\n   - Before:\n     ```\n     main: {\n       plugins: [externalizeDepsPlugin()],\n       build: {\n         outDir: 'dist/main',\n         rollupOptions: {\n           external: ['better-sqlite3', '@anthropic-ai/claude-agent-sdk'],\n         },\n       },\n     },\n     ```\n   - After:\n     ```\n     main: {\n       build: {\n         outDir: 'dist/main',\n         rollupOptions: {\n           external: ['better-sqlite3', '@anthropic-ai/claude-agent-sdk'],\n         },\n       },\n     },\n     ```\n\n3. **`preload` config block** — remove the `plugins` array entirely:\n   - Before:\n     ```\n     preload: {\n       plugins: [externalizeDepsPlugin()],\n       build: {\n         outDir: 'dist/preload',\n       },\n     },\n     ```\n   - After:\n     ```\n     preload: {\n       build: {\n         outDir: 'dist/preload',\n       },\n     },\n     ```\n\nDo NOT touch:\n- `rollupOptions.external: ['better-sqlite3', '@anthropic-ai/claude-agent-sdk']` in the `main` block — these Rollup-level externals are independent of `externalizeDepsPlugin` and must remain\n- The entire `renderer` section — unchanged\n- The `sharedSubpathAliases()` helper function — unchanged\n- The `react`, `tailwindcss`, `path`, `fs` imports — unchanged",
      "activeForm": "Removing externalizeDepsPlugin from apps/desktop/electron.vite.config.ts",
      "size": "small",
      "priority": "high",
      "dependencies": [],
      "parallelWith": ["1.1"]
    },
    {
      "id": "1.3",
      "phase": 1,
      "phaseName": "Implementation",
      "subject": "[upgrade-electron-vite] [P1] Regenerate pnpm lockfile after version bumps",
      "description": "Run `pnpm install` from the repo root to regenerate `pnpm-lock.yaml` with the updated electron-vite, electron, and @tailwindcss/vite versions.\n\nCommand:\n```bash\npnpm install\n```\n\nRun from: repo root (`/path/to/dork-os/core/`), not from `apps/desktop/`.\n\nExpected outcome: `pnpm-lock.yaml` is updated; no install errors.\n\n**Native module note:** Bumping Electron from 33 to 39 changes the ABI (Application Binary Interface). The `better-sqlite3` native binary compiled for Electron 33 will not load in Electron 39. `electron-builder` handles the production rebuild automatically via `npmRebuild: true` in `electron-builder.yml`. For dev mode, the rebuild happens via `@electron/rebuild` on first launch — but only if a `NODE_MODULE_VERSION` mismatch error appears in the Electron dev console. If it does, run:\n```bash\ncd apps/desktop && npx @electron/rebuild --version 39.8.5\n```\nDo not pre-emptively run this — wait to see if the error surfaces in task 1.4 verification.",
      "activeForm": "Regenerating pnpm-lock.yaml",
      "size": "small",
      "priority": "high",
      "dependencies": ["1.1", "1.2"],
      "parallelWith": []
    },
    {
      "id": "1.4",
      "phase": 1,
      "phaseName": "Implementation",
      "subject": "[upgrade-electron-vite] [P1] Verify the upgrade — build, typecheck, lint, dev, and pack",
      "description": "Run all verification steps in order. Every check must pass before the upgrade is considered complete.\n\n**Step 1 — Build:**\n```bash\npnpm --filter @dorkos/desktop build\n```\nPass condition: exits 0; `apps/desktop/dist/` is populated with `main/`, `preload/`, and `renderer/` subdirectories.\n\n**Step 2 — TypeScript:**\n```bash\npnpm typecheck\n```\nPass condition: zero errors across all packages in the monorepo.\n\n**Step 3 — Lint:**\n```bash\npnpm lint\n```\nPass condition: zero errors. (The removal of `externalizeDepsPlugin` from the import in task 1.2 eliminates any `no-unused-vars` or unused-import warnings that might have appeared.)\n\n**Step 4 — Dev mode:**\n```bash\npnpm --filter @dorkos/desktop dev\n```\nPass condition: Electron window opens, React app loads in the renderer, HMR is functional (edit a source file in `apps/client/src/` and confirm the renderer hot-reloads without a full restart).\n\n**Step 5 — Native module check (during dev mode):**\nOpen the Electron dev console (View → Toggle Developer Tools in the Electron menu, or Cmd+Option+I). Confirm there is no `NODE_MODULE_VERSION` mismatch error. If one appears, quit dev mode, run the following, then restart dev:\n```bash\ncd apps/desktop && npx @electron/rebuild --version 39.8.5\n```\n\n**Step 6 — Pack:**\n```bash\npnpm --filter @dorkos/desktop pack\n```\nPass condition: unsigned `.app` bundle produced at `apps/desktop/release/mac-arm64/DorkOS.app` (or equivalent path under `release/`). No code signing required — `CSC_IDENTITY_AUTO_DISCOVERY=false` is already set in the `pack` script.\n\nAll six steps must pass. If any step fails, investigate and fix before marking complete.",
      "activeForm": "Verifying build, typecheck, lint, dev mode, and pack",
      "size": "small",
      "priority": "high",
      "dependencies": ["1.3"],
      "parallelWith": []
    }
  ]
}

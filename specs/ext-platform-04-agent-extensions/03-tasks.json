{
  "spec": "specs/ext-platform-04-agent-extensions/02-specification.md",
  "slug": "ext-platform-04-agent-extensions",
  "generatedAt": "2026-03-27T00:00:00Z",
  "mode": "full",
  "lastDecomposeDate": null,
  "tasks": [
    {
      "id": "1.1",
      "phase": 1,
      "phaseName": "Core MCP Tools",
      "subject": "[ext-platform-04-agent-extensions] [P1] Add extensionManager to McpToolDeps and wire it in mcp-server.ts",
      "description": "Add the `extensionManager` field to the `McpToolDeps` interface and wire the actual `ExtensionManager` instance into the deps object at server startup.\n\n## File: `apps/server/src/services/runtimes/claude-code/mcp-tools/types.ts`\n\nAdd a new optional field to `McpToolDeps`:\n\n```typescript\nimport type { ExtensionManager } from '../../../extensions/extension-manager.js';\n\nexport interface McpToolDeps {\n  transcriptReader: TranscriptReader;\n  defaultCwd: string;\n  pulseStore?: PulseStore;\n  relayCore?: RelayCore;\n  adapterManager?: AdapterManager;\n  traceStore?: TraceStore;\n  bindingStore?: BindingStore;\n  bindingRouter?: BindingRouter;\n  meshCore?: MeshCore;\n  /** Optional ExtensionManager — undefined when extensions are disabled */\n  extensionManager?: ExtensionManager;\n}\n```\n\n## File: `apps/server/src/index.ts`\n\nIn the `mcpToolDeps` assembly block (around line 257), add the extensionManager spread:\n\n```typescript\nconst mcpToolDeps = {\n  transcriptReader: claudeRuntime.getTranscriptReader(),\n  defaultCwd: env.DORKOS_DEFAULT_CWD ?? process.cwd(),\n  ...(pulseStore && { pulseStore }),\n  ...(relayCore && { relayCore }),\n  ...(adapterManager && { adapterManager }),\n  ...(adapterManager && { bindingStore: adapterManager.getBindingStore() }),\n  ...(adapterManager && { bindingRouter: adapterManager.getBindingRouter() }),\n  ...(traceStore && { traceStore }),\n  ...(meshCore && { meshCore }),\n  ...(extensionManager && { extensionManager }),  // <-- NEW\n};\n```\n\nThe `extensionManager` variable is already declared at module scope (line 53) and initialized in the startup sequence (line 95). The spread pattern `...(extensionManager && { extensionManager })` is consistent with how `pulseStore`, `relayCore`, and `meshCore` are conditionally included.\n\n## Verification\n\n1. Run `pnpm typecheck` — the `McpToolDeps` type should compile with the new optional field\n2. Start the dev server (`pnpm dev`) — confirm no startup errors\n3. Verify that extension tools registered in later tasks receive `deps.extensionManager` when the extension system initializes successfully\n\n## Acceptance Criteria\n\n- `McpToolDeps` interface includes `extensionManager?: ExtensionManager`\n- The `import type` for `ExtensionManager` is present in `types.ts`\n- `mcpToolDeps` in `index.ts` conditionally includes `extensionManager`\n- `pnpm typecheck` passes\n- No runtime errors on server startup",
      "activeForm": "Adding extensionManager to McpToolDeps and wiring it in index.ts",
      "size": "small",
      "priority": "high",
      "dependencies": [],
      "parallelWith": []
    },
    {
      "id": "1.2",
      "phase": 1,
      "phaseName": "Core MCP Tools",
      "subject": "[ext-platform-04-agent-extensions] [P1] Implement list_extensions MCP tool handler",
      "description": "Create the `extension-tools.ts` file with the `list_extensions` handler and register it in `mcp-server.ts`. This file will be the home for all 6 extension MCP tool handlers.\n\n## File: `apps/server/src/services/runtimes/claude-code/mcp-tools/extension-tools.ts` (NEW)\n\n```typescript\nimport { z } from 'zod';\nimport type { McpToolDeps } from './types.js';\nimport { jsonContent } from './types.js';\n\n/**\n * Guard that checks if the extension manager is available.\n * Returns the manager if present, or an error response if not.\n */\nfunction requireExtensionManager(deps: McpToolDeps) {\n  if (!deps.extensionManager) {\n    return null;\n  }\n  return deps.extensionManager;\n}\n\n/**\n * Handler factory for `list_extensions` — lists all discovered extensions\n * with their status, scope, and errors.\n */\nexport function createListExtensionsHandler(deps: McpToolDeps) {\n  return async () => {\n    const manager = requireExtensionManager(deps);\n    if (!manager) {\n      return jsonContent({ error: 'Extension system is not available' }, true);\n    }\n    const extensions = manager.listPublic().map((ext) => ({\n      id: ext.id,\n      name: ext.manifest.name,\n      version: ext.manifest.version,\n      status: ext.status,\n      scope: ext.scope,\n      bundleReady: ext.bundleReady,\n      ...(ext.manifest.description && { description: ext.manifest.description }),\n      ...(ext.error && { error: ext.error }),\n    }));\n    return jsonContent({ extensions, count: extensions.length });\n  };\n}\n```\n\nThe response includes per-extension: `id`, `name` (from manifest), `version` (from manifest), `status` (ExtensionStatus enum value), `scope` (`'global'` or `'local'`), `bundleReady` (boolean), optionally `description` (from manifest) and `error` (structured error object with `code`, `message`, `details`). The `path` field from `ExtensionRecord` is intentionally excluded from the MCP response to avoid exposing server-internal filesystem details.\n\n## File: `apps/server/src/services/core/mcp-server.ts`\n\nAdd the import and registration at the bottom of the tool registrations:\n\n```typescript\nimport {\n  createListExtensionsHandler,\n} from '../runtimes/claude-code/mcp-tools/extension-tools.js';\n\n// Inside createExternalMcpServer(), after the Agent tools section:\n\n// ── Extension tools ──────────────────────────────────────────────────\nserver.tool(\n  'list_extensions',\n  'List all discovered DorkOS extensions with their status, scope, and errors. Returns both global (~/.dork/extensions/) and local (.dork/extensions/ in active CWD) extensions.',\n  {},\n  createListExtensionsHandler(deps)\n);\n```\n\n## Acceptance Criteria\n\n- `extension-tools.ts` exists with `createListExtensionsHandler` exported\n- `list_extensions` tool is registered in `mcp-server.ts` with no parameters\n- When `extensionManager` is undefined, returns `{ error: 'Extension system is not available' }` with `isError: true`\n- When extensions exist, returns `{ extensions: [...], count: N }` with all specified fields\n- Empty extension list returns `{ extensions: [], count: 0 }`\n- `pnpm typecheck` passes",
      "activeForm": "Implementing list_extensions MCP tool handler",
      "size": "small",
      "priority": "high",
      "dependencies": ["1.1"],
      "parallelWith": []
    },
    {
      "id": "1.3",
      "phase": 1,
      "phaseName": "Core MCP Tools",
      "subject": "[ext-platform-04-agent-extensions] [P1] Implement createExtension() method on ExtensionManager",
      "description": "Add the `createExtension()` method to `ExtensionManager` that scaffolds a new extension directory with a manifest and template starter code, then compiles and enables it.\n\n## File: `apps/server/src/services/extensions/extension-manager.ts`\n\nAdd the following imports at the top:\n\n```typescript\nimport fs from 'fs/promises';\nimport path from 'path';\n```\n\nAdd a new result type and the method to `ExtensionManager`:\n\n```typescript\n/** Result of creating a new extension. */\nexport interface CreateExtensionResult {\n  id: string;\n  path: string;\n  scope: 'global' | 'local';\n  template: string;\n  status: ExtensionStatus;\n  bundleReady: boolean;\n  files: string[];\n  error?: {\n    code: string;\n    message: string;\n    errors?: Array<{\n      text: string;\n      location?: { file: string; line: number; column: number };\n    }>;\n  };\n}\n```\n\nThe `createExtension()` method:\n\n```typescript\n/**\n * Scaffold a new extension directory with manifest and starter code.\n *\n * @param options - Creation parameters\n * @returns Created extension info including compilation result\n */\nasync createExtension(options: {\n  name: string;\n  description?: string;\n  template: 'dashboard-card' | 'command' | 'settings-panel';\n  scope: 'global' | 'local';\n}): Promise<CreateExtensionResult> {\n  const { name, description, template, scope } = options;\n\n  // 1. Resolve target directory\n  let targetDir: string;\n  if (scope === 'local') {\n    if (!this.currentCwd) {\n      throw new Error('Cannot create local extension: no working directory is active');\n    }\n    targetDir = path.join(this.currentCwd, '.dork', 'extensions', name);\n  } else {\n    targetDir = path.join(this.dorkHome, 'extensions', name);\n  }\n\n  // 2. Check directory does not exist\n  try {\n    await fs.access(targetDir);\n    throw new Error(`Extension '${name}' already exists at ${targetDir}`);\n  } catch (err) {\n    if (err instanceof Error && err.message.includes('already exists')) throw err;\n    // ENOENT is expected — directory doesn't exist yet\n  }\n\n  // 3. Create directory\n  await fs.mkdir(targetDir, { recursive: true });\n\n  // 4. Write extension.json\n  const manifest = generateManifest(name, description, template);\n  await fs.writeFile(\n    path.join(targetDir, 'extension.json'),\n    JSON.stringify(manifest, null, 2),\n    'utf-8'\n  );\n\n  // 5. Write index.ts from template\n  const indexContent = generateTemplate(name, description ?? '', template);\n  await fs.writeFile(path.join(targetDir, 'index.ts'), indexContent, 'utf-8');\n\n  // 6. Reload to discover the new extension\n  await this.reload();\n\n  // 7. Enable (compile + add to config)\n  const enableResult = await this.enable(name);\n\n  // 8. Build result\n  const record = this.extensions.get(name);\n  const result: CreateExtensionResult = {\n    id: name,\n    path: targetDir,\n    scope,\n    template,\n    status: record?.status ?? 'compile_error',\n    bundleReady: record?.bundleReady ?? false,\n    files: ['extension.json', 'index.ts'],\n  };\n\n  if (record?.error) {\n    result.error = {\n      code: record.error.code,\n      message: record.error.message,\n      ...(record.error.details && {\n        errors: record.error.details.split('\\n').map((text) => ({ text })),\n      }),\n    };\n  }\n\n  return result;\n}\n```\n\nThe `dorkHome` field needs to be stored. Add it to the constructor:\n\n```typescript\nprivate dorkHome: string;\n\nconstructor(dorkHome: string) {\n  this.dorkHome = dorkHome;  // Store for createExtension()\n  this.discovery = new ExtensionDiscovery(dorkHome);\n  this.compiler = new ExtensionCompiler(dorkHome);\n}\n```\n\n## Template and Manifest Generators\n\nAdd these as module-level functions in `extension-manager.ts` (or a separate `extension-templates.ts` if the file gets large):\n\n```typescript\n/** Convert kebab-case to PascalCase: \"github-prs\" -> \"GithubPrs\" */\nfunction toPascalCase(str: string): string {\n  return str\n    .split('-')\n    .map((s) => s.charAt(0).toUpperCase() + s.slice(1))\n    .join('');\n}\n\n/** Convert kebab-case to Title Case: \"github-prs\" -> \"Github Prs\" */\nfunction toTitleCase(str: string): string {\n  return str\n    .split('-')\n    .map((s) => s.charAt(0).toUpperCase() + s.slice(1))\n    .join(' ');\n}\n\n/** Generate the extension.json manifest content. */\nfunction generateManifest(\n  name: string,\n  description?: string,\n  template?: string\n): object {\n  const contributions: Record<string, boolean> = {};\n  switch (template) {\n    case 'dashboard-card':\n      contributions['dashboard.sections'] = true;\n      break;\n    case 'command':\n      contributions['command-palette.items'] = true;\n      break;\n    case 'settings-panel':\n      contributions['settings.tabs'] = true;\n      break;\n  }\n\n  return {\n    id: name,\n    name: toTitleCase(name),\n    version: '0.1.0',\n    ...(description && { description }),\n    author: 'agent',\n    minHostVersion: '0.1.0',\n    contributions,\n  };\n}\n\n/** Select and generate the index.ts template content. */\nfunction generateTemplate(\n  name: string,\n  description: string,\n  template: 'dashboard-card' | 'command' | 'settings-panel'\n): string {\n  switch (template) {\n    case 'dashboard-card':\n      return generateDashboardCardTemplate(name, description);\n    case 'command':\n      return generateCommandTemplate(name, description);\n    case 'settings-panel':\n      return generateSettingsPanelTemplate(name, description);\n  }\n}\n```\n\nThe three template generators produce the exact code shown in the specification sections 5.2, 5.3, and 5.4. Each template includes:\n\n- A header comment block with the extension name, description, and a compact API Quick Reference listing the most commonly used methods and all available slot names\n- An import of `ExtensionAPI` type from `@dorkos/extension-api`\n- A component function (for dashboard-card and settings-panel) or just registration code (for command)\n- An `activate(api)` function that registers the contribution in the appropriate slot\n\n### `dashboard-card` template (default):\n\nProduces a React component `{PascalCaseName}Section` with inline styles (padding, border, border-radius) and an `activate()` function that calls `api.registerComponent('dashboard.sections', '{name}-section', Component, { priority: 50 })`.\n\n### `command` template:\n\nProduces an `activate()` function that calls `api.registerCommand('{name}-run', '{Title Case Name}', callback, { icon: 'terminal' })` where the callback calls `api.notify('{Title Case Name} executed!', { type: 'success' })`.\n\n### `settings-panel` template:\n\nProduces a React component `{PascalCaseName}Settings` with a heading and description, and an `activate()` function that calls `api.registerSettingsTab('{name}-settings', '{Title Case Name}', Component)`.\n\n## Error Handling\n\n- If directory already exists: throw `new Error(\\`Extension '${name}' already exists at ${targetDir}\\`)`\n- If scope is local with no CWD: throw `new Error('Cannot create local extension: no working directory is active')`\n- If filesystem write fails: propagate the OS error (caller handles)\n- If compilation fails after scaffolding: return the result with `status: 'compile_error'` and error details — the files remain on disk for the agent to fix\n\n## Acceptance Criteria\n\n- `createExtension()` creates the directory, `extension.json`, and `index.ts` on disk\n- Three templates produce valid TypeScript that compiles with esbuild\n- Local scope uses `{currentCwd}/.dork/extensions/{name}/`\n- Global scope uses `{dorkHome}/extensions/{name}/`\n- Throws descriptive error when directory already exists\n- Throws descriptive error when local scope requested with no CWD\n- After creation, the extension is discovered, compiled, and added to the enabled list\n- Generated manifest includes correct `contributions` for the template type\n- `pnpm typecheck` passes",
      "activeForm": "Implementing createExtension() method with templates on ExtensionManager",
      "size": "large",
      "priority": "high",
      "dependencies": ["1.1"],
      "parallelWith": []
    },
    {
      "id": "1.4",
      "phase": 1,
      "phaseName": "Core MCP Tools",
      "subject": "[ext-platform-04-agent-extensions] [P1] Implement create_extension MCP tool handler",
      "description": "Add the `create_extension` MCP tool handler to `extension-tools.ts` and register it in `mcp-server.ts`.\n\n## File: `apps/server/src/services/runtimes/claude-code/mcp-tools/extension-tools.ts`\n\nAdd the handler factory:\n\n```typescript\n/**\n * Handler factory for `create_extension` — scaffolds a new extension\n * with manifest and working starter code.\n */\nexport function createCreateExtensionHandler(deps: McpToolDeps) {\n  return async (args: {\n    name: string;\n    description?: string;\n    template?: 'dashboard-card' | 'command' | 'settings-panel';\n    scope?: 'global' | 'local';\n  }) => {\n    const manager = requireExtensionManager(deps);\n    if (!manager) {\n      return jsonContent({ error: 'Extension system is not available' }, true);\n    }\n\n    try {\n      const result = await manager.createExtension({\n        name: args.name,\n        description: args.description,\n        template: args.template ?? 'dashboard-card',\n        scope: args.scope ?? 'local',\n      });\n\n      return jsonContent({\n        created: true,\n        ...result,\n        message: result.bundleReady\n          ? `Extension '${result.id}' created and compiled successfully. Edit index.ts and call reload_extensions to see changes.`\n          : `Extension '${result.id}' created but compilation failed. Check the error and fix index.ts.`,\n      });\n    } catch (err) {\n      return jsonContent(\n        { error: err instanceof Error ? err.message : 'Failed to create extension' },\n        true\n      );\n    }\n  };\n}\n```\n\n## File: `apps/server/src/services/core/mcp-server.ts`\n\nAdd the import and registration:\n\n```typescript\nimport {\n  createListExtensionsHandler,\n  createCreateExtensionHandler,\n} from '../runtimes/claude-code/mcp-tools/extension-tools.js';\n\n// In createExternalMcpServer(), after list_extensions:\n\nserver.tool(\n  'create_extension',\n  'Scaffold a new DorkOS extension with a working starter that compiles and activates. Creates an extension.json manifest and an index.ts entry point in the extensions directory. The extension is automatically enabled and compiled.',\n  {\n    name: z.string()\n      .min(1)\n      .regex(/^[a-z0-9][a-z0-9-]*$/)\n      .describe('Extension ID (kebab-case, e.g. \"github-prs\")'),\n    description: z.string()\n      .optional()\n      .describe('Short description of what the extension does'),\n    template: z.enum(['dashboard-card', 'command', 'settings-panel'])\n      .optional()\n      .describe('Starter template: dashboard-card (default), command, or settings-panel'),\n    scope: z.enum(['global', 'local'])\n      .optional()\n      .describe('Where to create: local (.dork/extensions/ in CWD) or global (~/.dork/extensions/)'),\n  },\n  createCreateExtensionHandler(deps)\n);\n```\n\n## Parameters\n\n| Parameter | Type | Required | Default | Description |\n|-----------|------|----------|---------|-------------|\n| `name` | string | Yes | — | Extension ID (kebab-case, e.g. \"github-prs\"). Must match `/^[a-z0-9][a-z0-9-]*$/`. |\n| `description` | string | No | — | Short description of what the extension does |\n| `template` | enum | No | `'dashboard-card'` | Starter template: `dashboard-card`, `command`, or `settings-panel` |\n| `scope` | enum | No | `'local'` | Where to create: `local` (`.dork/extensions/` in CWD) or `global` (`~/.dork/extensions/`) |\n\n## Return Shape (success)\n\n```json\n{\n  \"created\": true,\n  \"id\": \"github-prs\",\n  \"path\": \"/Users/kai/projects/myapp/.dork/extensions/github-prs\",\n  \"scope\": \"local\",\n  \"template\": \"dashboard-card\",\n  \"status\": \"compiled\",\n  \"bundleReady\": true,\n  \"files\": [\"extension.json\", \"index.ts\"],\n  \"message\": \"Extension 'github-prs' created and compiled successfully. Edit index.ts and call reload_extensions to see changes.\"\n}\n```\n\n## Error Cases\n\n- Extension system not available: `isError: true` with `{ error: 'Extension system is not available' }`\n- Extension directory already exists: `isError: true` with `{ error: \"Extension 'name' already exists at /path\" }`\n- Local scope requested but no CWD active: `isError: true` with `{ error: 'Cannot create local extension: no working directory is active' }`\n- Compilation failure after scaffold: `created: true` but `status: 'compile_error'` with error details\n\n## Acceptance Criteria\n\n- `create_extension` tool is registered in `mcp-server.ts` with the Zod parameter schema\n- Default template is `dashboard-card`, default scope is `local`\n- Extension name validation enforces kebab-case via regex\n- On success, returns `created: true` with path, scope, template, status, bundleReady, files, and a helpful message\n- On directory conflict, returns `isError: true` with descriptive error\n- On missing CWD for local scope, returns `isError: true`\n- `pnpm typecheck` passes",
      "activeForm": "Implementing create_extension MCP tool handler",
      "size": "medium",
      "priority": "high",
      "dependencies": ["1.2", "1.3"],
      "parallelWith": []
    },
    {
      "id": "1.5",
      "phase": 1,
      "phaseName": "Core MCP Tools",
      "subject": "[ext-platform-04-agent-extensions] [P1] Implement reloadExtension(id) method on ExtensionManager",
      "description": "Add a per-extension reload method to `ExtensionManager` that recompiles a single extension and returns structured results. This enables targeted hot reload without re-scanning all extensions.\n\n## File: `apps/server/src/services/extensions/extension-manager.ts`\n\nAdd the result type:\n\n```typescript\n/** Result of reloading a single extension. */\nexport interface ReloadExtensionResult {\n  id: string;\n  status: ExtensionStatus;\n  bundleReady: boolean;\n  sourceHash?: string;\n  error?: {\n    code: string;\n    message: string;\n    errors?: Array<{\n      text: string;\n      location?: { file: string; line: number; column: number };\n    }>;\n  };\n}\n```\n\nAdd the method to the `ExtensionManager` class:\n\n```typescript\n/**\n * Reload a single extension: recompile and update its record.\n * Used for per-extension hot reload when the agent edits one extension.\n *\n * @param id - Extension identifier\n * @returns Structured reload result for the single extension\n */\nasync reloadExtension(id: string): Promise<ReloadExtensionResult> {\n  const record = this.extensions.get(id);\n  if (!record) {\n    throw new Error(`Extension '${id}' not found`);\n  }\n\n  // Recompile the extension\n  const compileResult = await this.compiler.compile(record);\n\n  if ('error' in compileResult) {\n    record.status = 'compile_error';\n    record.error = {\n      code: compileResult.error.code,\n      message: compileResult.error.message,\n      details: compileResult.error.errors.map((e) => e.text).join('\\n'),\n    };\n    record.sourceHash = compileResult.sourceHash;\n    record.bundleReady = false;\n\n    return {\n      id,\n      status: 'compile_error',\n      bundleReady: false,\n      sourceHash: compileResult.sourceHash,\n      error: {\n        code: compileResult.error.code,\n        message: compileResult.error.message,\n        errors: compileResult.error.errors,\n      },\n    };\n  }\n\n  // Compilation succeeded\n  record.status = 'compiled';\n  record.sourceHash = compileResult.sourceHash;\n  record.bundleReady = true;\n  record.error = undefined;\n\n  return {\n    id,\n    status: 'compiled',\n    bundleReady: true,\n    sourceHash: compileResult.sourceHash,\n  };\n}\n```\n\n## Behavior Details\n\n1. Look up the extension record via `this.extensions.get(id)`. Throw if not found.\n2. Call `this.compiler.compile(record)` to recompile. The compiler reads the source file fresh from disk and computes a new content hash. If the hash matches a cached bundle, compilation returns instantly (cache hit).\n3. Update the record's status, sourceHash, bundleReady, and error fields based on the compile result.\n4. If compilation succeeds, clear any previous error on the record.\n5. Return structured result. The caller (MCP tool handler) is responsible for broadcasting the SSE event after this method completes.\n\n## Difference from `reload()`\n\nThe existing `reload()` method re-discovers ALL extensions from disk and recompiles ALL enabled ones. `reloadExtension(id)` skips the discovery step and only recompiles the single specified extension. This is faster and preserves state in other extensions.\n\n## Error Handling\n\n- Unknown extension ID: throws `new Error(\\`Extension '${id}' not found\\`)`\n- Compilation failure: returns the result with `status: 'compile_error'` and structured error array with file, line, column, text\n- Filesystem read failure during compilation: the compiler handles this internally and returns it as a compilation error\n\n## Acceptance Criteria\n\n- `reloadExtension(id)` compiles only the specified extension\n- Returns `ReloadExtensionResult` with status, bundleReady, optional sourceHash and error\n- Compilation errors include structured error array with `text` and optional `location` (file, line, column)\n- Throws when extension ID is not found\n- Updates the in-memory `ExtensionRecord` with the compilation result\n- Clears previous error when compilation succeeds\n- `pnpm typecheck` passes",
      "activeForm": "Implementing reloadExtension(id) per-extension reload method",
      "size": "medium",
      "priority": "high",
      "dependencies": ["1.1"],
      "parallelWith": ["1.3"]
    },
    {
      "id": "1.6",
      "phase": 1,
      "phaseName": "Core MCP Tools",
      "subject": "[ext-platform-04-agent-extensions] [P1] Implement reload_extensions MCP tool handler",
      "description": "Add the `reload_extensions` MCP tool handler to `extension-tools.ts` and register it in `mcp-server.ts`. This tool recompiles and reloads extensions, supporting both all-extension reload and single-extension targeted reload.\n\n## File: `apps/server/src/services/runtimes/claude-code/mcp-tools/extension-tools.ts`\n\n```typescript\n/**\n * Handler factory for `reload_extensions` — recompile and reload extensions.\n * With no arguments, reloads all. With an `id`, reloads only that extension.\n */\nexport function createReloadExtensionsHandler(deps: McpToolDeps) {\n  return async (args: { id?: string }) => {\n    const manager = requireExtensionManager(deps);\n    if (!manager) {\n      return jsonContent({ error: 'Extension system is not available' }, true);\n    }\n\n    try {\n      if (args.id) {\n        // Single-extension reload\n        const result = await manager.reloadExtension(args.id);\n        return jsonContent({\n          results: { [result.id]: result },\n          summary: {\n            total: 1,\n            compiled: result.status === 'compiled' ? 1 : 0,\n            errors: result.status === 'compile_error' ? 1 : 0,\n          },\n        });\n      }\n\n      // Full reload\n      const extensions = await manager.reload();\n      const results: Record<string, unknown> = {};\n      let compiledCount = 0;\n      let errorCount = 0;\n\n      for (const ext of extensions) {\n        const entry: Record<string, unknown> = {\n          status: ext.status,\n          bundleReady: ext.bundleReady,\n        };\n        if (ext.error) {\n          entry.error = ext.error;\n          errorCount++;\n        }\n        if (ext.status === 'compiled') {\n          compiledCount++;\n        }\n        results[ext.id] = entry;\n      }\n\n      return jsonContent({\n        results,\n        summary: {\n          total: extensions.length,\n          compiled: compiledCount,\n          errors: errorCount,\n        },\n      });\n    } catch (err) {\n      return jsonContent(\n        { error: err instanceof Error ? err.message : 'Failed to reload extensions' },\n        true\n      );\n    }\n  };\n}\n```\n\n## File: `apps/server/src/services/core/mcp-server.ts`\n\nAdd to the imports and register:\n\n```typescript\nimport {\n  createListExtensionsHandler,\n  createCreateExtensionHandler,\n  createReloadExtensionsHandler,\n} from '../runtimes/claude-code/mcp-tools/extension-tools.js';\n\nserver.tool(\n  'reload_extensions',\n  'Recompile and reload extensions. With no arguments, reloads all extensions. With an id, reloads only that extension (per-extension hot reload). Returns per-extension structured results including any compilation or activation errors.',\n  {\n    id: z.string()\n      .optional()\n      .describe('Extension ID to reload. Omit to reload all extensions.'),\n  },\n  createReloadExtensionsHandler(deps)\n);\n```\n\n## Return Shape (all extensions, success)\n\n```json\n{\n  \"results\": {\n    \"github-prs\": { \"status\": \"compiled\", \"bundleReady\": true },\n    \"my-commands\": { \"status\": \"compiled\", \"bundleReady\": true }\n  },\n  \"summary\": { \"total\": 2, \"compiled\": 2, \"errors\": 0 }\n}\n```\n\n## Return Shape (with errors)\n\n```json\n{\n  \"results\": {\n    \"github-prs\": {\n      \"status\": \"compile_error\",\n      \"bundleReady\": false,\n      \"error\": {\n        \"code\": \"compilation_failed\",\n        \"message\": \"Compilation failed for github-prs\",\n        \"errors\": [{ \"text\": \"Property 'registerWidget' does not exist\", \"location\": { \"file\": \"index.ts\", \"line\": 15, \"column\": 8 } }]\n      }\n    }\n  },\n  \"summary\": { \"total\": 1, \"compiled\": 0, \"errors\": 1 }\n}\n```\n\n## Return Shape (single extension, not found)\n\n```json\n{ \"error\": \"Extension 'nonexistent' not found\" }\n```\n\n## Error Cases\n\n- Extension system not available: `isError: true`\n- Single-extension reload with unknown ID: `isError: true` (thrown from `reloadExtension()`)\n- Filesystem errors during discovery: included in per-extension error results\n\n## Acceptance Criteria\n\n- `reload_extensions` tool is registered with optional `id` parameter\n- When `id` is provided, calls `reloadExtension(id)` for targeted reload\n- When `id` is omitted, calls `reload()` for full reload\n- Returns `{ results: {...}, summary: { total, compiled, errors } }` format\n- Per-extension results include `status`, `bundleReady`, and optionally `error` with structured error array\n- Unknown extension ID returns `isError: true`\n- `pnpm typecheck` passes",
      "activeForm": "Implementing reload_extensions MCP tool handler",
      "size": "medium",
      "priority": "high",
      "dependencies": ["1.2", "1.5"],
      "parallelWith": []
    },
    {
      "id": "1.7",
      "phase": 1,
      "phaseName": "Core MCP Tools",
      "subject": "[ext-platform-04-agent-extensions] [P1] Implement get_extension_errors MCP tool handler",
      "description": "Add the `get_extension_errors` MCP tool handler to `extension-tools.ts` and register it in `mcp-server.ts`. This tool provides detailed error information for a specific extension.\n\n## File: `apps/server/src/services/runtimes/claude-code/mcp-tools/extension-tools.ts`\n\n```typescript\n/**\n * Handler factory for `get_extension_errors` — returns detailed compilation\n * or runtime errors for a specific extension.\n */\nexport function createGetExtensionErrorsHandler(deps: McpToolDeps) {\n  return async (args: { id: string }) => {\n    const manager = requireExtensionManager(deps);\n    if (!manager) {\n      return jsonContent({ error: 'Extension system is not available' }, true);\n    }\n\n    const extensions = manager.listPublic();\n    const ext = extensions.find((e) => e.id === args.id);\n    if (!ext) {\n      return jsonContent({ error: `Extension '${args.id}' not found` }, true);\n    }\n\n    return jsonContent({\n      id: ext.id,\n      status: ext.status,\n      error: ext.error ?? null,\n    });\n  };\n}\n```\n\n## File: `apps/server/src/services/core/mcp-server.ts`\n\nAdd to the imports and register:\n\n```typescript\nimport {\n  createListExtensionsHandler,\n  createCreateExtensionHandler,\n  createReloadExtensionsHandler,\n  createGetExtensionErrorsHandler,\n} from '../runtimes/claude-code/mcp-tools/extension-tools.js';\n\nserver.tool(\n  'get_extension_errors',\n  'Get detailed compilation or runtime errors for a specific extension. Use this after reload_extensions reports errors to get the full error context including source locations.',\n  {\n    id: z.string().describe('Extension ID to get errors for'),\n  },\n  createGetExtensionErrorsHandler(deps)\n);\n```\n\n## Return Shape (has errors)\n\n```json\n{\n  \"id\": \"github-prs\",\n  \"status\": \"compile_error\",\n  \"error\": {\n    \"code\": \"compilation_failed\",\n    \"message\": \"Compilation failed for github-prs\",\n    \"details\": \"Property 'registerWidget' does not exist on type 'ExtensionAPI'\"\n  }\n}\n```\n\n## Return Shape (no errors)\n\n```json\n{\n  \"id\": \"github-prs\",\n  \"status\": \"compiled\",\n  \"error\": null\n}\n```\n\n## Return Shape (not found)\n\n```json\n{ \"error\": \"Extension 'nonexistent' not found\" }\n```\n\nThe error structure mirrors what the `ExtensionRecordPublic` carries: `code` (string identifying the error category), `message` (human-readable summary), and optionally `details` (individual error entries). For `compile_error` status, the error comes from esbuild via `extension-compiler.ts`. For `activate_error` status, it comes from the client-reported runtime error.\n\n## Acceptance Criteria\n\n- `get_extension_errors` tool is registered with required `id` parameter\n- Returns `{ id, status, error }` where error is the structured error object or `null`\n- Unknown extension ID returns `isError: true`\n- Extension system not available returns `isError: true`\n- `pnpm typecheck` passes",
      "activeForm": "Implementing get_extension_errors MCP tool handler",
      "size": "small",
      "priority": "medium",
      "dependencies": ["1.2"],
      "parallelWith": ["1.4", "1.5", "1.6"]
    },
    {
      "id": "1.8",
      "phase": 1,
      "phaseName": "Core MCP Tools",
      "subject": "[ext-platform-04-agent-extensions] [P1] Unit tests for Phase 1 MCP tools and ExtensionManager methods",
      "description": "Write unit tests for all Phase 1 MCP tool handlers and the new `createExtension()` and `reloadExtension()` methods on `ExtensionManager`.\n\n## File: `apps/server/src/services/runtimes/claude-code/mcp-tools/__tests__/extension-tools.test.ts` (NEW)\n\nTest each handler factory with a mock `ExtensionManager`. Create the mock as:\n\n```typescript\nimport { describe, it, expect, vi, beforeEach } from 'vitest';\nimport {\n  createListExtensionsHandler,\n  createCreateExtensionHandler,\n  createReloadExtensionsHandler,\n  createGetExtensionErrorsHandler,\n} from '../extension-tools.js';\nimport type { McpToolDeps } from '../types.js';\n\nfunction createMockExtensionManager() {\n  return {\n    listPublic: vi.fn().mockReturnValue([]),\n    get: vi.fn().mockReturnValue(undefined),\n    reload: vi.fn().mockResolvedValue([]),\n    enable: vi.fn().mockResolvedValue(null),\n    disable: vi.fn().mockResolvedValue(null),\n    createExtension: vi.fn().mockResolvedValue({}),\n    reloadExtension: vi.fn().mockResolvedValue({}),\n    testExtension: vi.fn().mockResolvedValue({}),\n  };\n}\n\nfunction createDeps(manager?: ReturnType<typeof createMockExtensionManager>): McpToolDeps {\n  return {\n    transcriptReader: {} as McpToolDeps['transcriptReader'],\n    defaultCwd: '/test',\n    ...(manager && { extensionManager: manager as unknown as McpToolDeps['extensionManager'] }),\n  };\n}\n```\n\n### Test Scenarios for `list_extensions`\n\n1. **Returns formatted extension list**: Mock `listPublic()` returning 2 extensions with different statuses. Verify response includes `extensions` array with `id`, `name`, `version`, `status`, `scope`, `bundleReady` fields, and `count: 2`.\n2. **Handles empty list**: Mock `listPublic()` returning `[]`. Verify `{ extensions: [], count: 0 }`.\n3. **Returns error when manager missing**: Create deps without `extensionManager`. Verify `isError: true` and `{ error: 'Extension system is not available' }`.\n4. **Includes description and error when present**: Mock extension with `manifest.description` and `error` object. Verify both fields appear in response.\n5. **Omits description when absent**: Mock extension without description. Verify `description` field is not in the response object.\n\n### Test Scenarios for `create_extension`\n\n1. **Creates extension with defaults**: Call with only `name`. Verify `createExtension()` called with `template: 'dashboard-card'` and `scope: 'local'`.\n2. **Passes all parameters**: Call with name, description, template, scope. Verify all passed through.\n3. **Returns success response**: Mock `createExtension()` returning `{ id: 'test', path: '/path', scope: 'local', template: 'dashboard-card', status: 'compiled', bundleReady: true, files: ['extension.json', 'index.ts'] }`. Verify response has `created: true` and a success `message`.\n4. **Returns error for existing directory**: Mock `createExtension()` throwing `\"Extension 'test' already exists\"`. Verify `isError: true`.\n5. **Returns error when manager missing**: Verify `isError: true` response.\n\n### Test Scenarios for `reload_extensions`\n\n1. **Full reload**: Call with no `id`. Verify `reload()` called (not `reloadExtension()`). Verify summary counts.\n2. **Single extension reload**: Call with `id: 'test'`. Verify `reloadExtension('test')` called.\n3. **Returns compilation errors**: Mock `reloadExtension()` returning `status: 'compile_error'`. Verify error included in results.\n4. **Returns not-found error**: Mock `reloadExtension()` throwing `\"Extension 'x' not found\"`. Verify `isError: true`.\n5. **Summary counts match results**: Full reload with 3 extensions (2 compiled, 1 error). Verify `summary.total === 3`, `summary.compiled === 2`, `summary.errors === 1`.\n\n### Test Scenarios for `get_extension_errors`\n\n1. **Returns errors for extension with compile_error**: Mock extension with error. Verify `error` field matches.\n2. **Returns null error for healthy extension**: Mock extension with `status: 'compiled'`. Verify `error: null`.\n3. **Returns not-found for unknown ID**: Verify `isError: true`.\n4. **Returns error when manager missing**: Verify `isError: true`.\n\n## File: `apps/server/src/services/extensions/__tests__/extension-manager-create.test.ts` (NEW)\n\nTest `createExtension()` with filesystem mocks:\n\n1. **Writes correct manifest for dashboard-card template**: Verify JSON content includes `contributions: { 'dashboard.sections': true }`\n2. **Writes correct manifest for command template**: Verify `contributions: { 'command-palette.items': true }`\n3. **Writes correct manifest for settings-panel template**: Verify `contributions: { 'settings.tabs': true }`\n4. **Creates directory at correct path (local)**: Verify path is `{cwd}/.dork/extensions/{name}/`\n5. **Creates directory at correct path (global)**: Verify path is `{dorkHome}/extensions/{name}/`\n6. **Throws when directory exists**: Mock `fs.access` succeeding (directory exists). Verify error message includes extension name.\n7. **Throws when local scope with no CWD**: Set `currentCwd` to null. Verify error message.\n8. **Triggers reload and enable after creation**: Verify `reload()` and `enable()` called in sequence.\n9. **Generated index.ts contains API quick reference comments**: Verify the file content includes \"ExtensionAPI Quick Reference\" comment block.\n10. **Generated index.ts contains correct activate function**: Verify the file content includes an `export function activate(api: ExtensionAPI)` line.\n\n## File: `apps/server/src/services/extensions/__tests__/extension-manager-reload.test.ts` (NEW)\n\nTest `reloadExtension()`:\n\n1. **Returns compiled status on success**: Mock compiler returning success. Verify `status: 'compiled'`, `bundleReady: true`.\n2. **Returns compile_error with structured errors**: Mock compiler returning error with location info. Verify error array with `text`, `location.file`, `location.line`, `location.column`.\n3. **Throws for unknown extension ID**: Verify error message includes the ID.\n4. **Updates in-memory record on success**: Verify `record.status`, `record.sourceHash`, `record.bundleReady` updated.\n5. **Clears error on successful recompile**: Set record error, then recompile successfully. Verify error is undefined.\n\n## Acceptance Criteria\n\n- All test files created and passing\n- Each handler tested with manager present and absent\n- Error paths covered (not-found, system unavailable, filesystem errors)\n- Mock strategy: `vi.fn()` stubs on ExtensionManager methods\n- `pnpm vitest run` passes for all three test files",
      "activeForm": "Writing unit tests for Phase 1 MCP tools and ExtensionManager methods",
      "size": "large",
      "priority": "high",
      "dependencies": ["1.4", "1.5", "1.6", "1.7"],
      "parallelWith": []
    },
    {
      "id": "2.1",
      "phase": 2,
      "phaseName": "Agent DX Improvements",
      "subject": "[ext-platform-04-agent-extensions] [P2] Implement get_extension_api MCP tool handler",
      "description": "Add the `get_extension_api` MCP tool handler that returns full ExtensionAPI type definitions and usage examples. This enables agents to understand the available API surface when writing extensions.\n\n## File: `apps/server/src/services/runtimes/claude-code/mcp-tools/extension-tools.ts`\n\nAdd a hardcoded string constant containing the full API reference. This is not dynamically read from disk — it is a static string that must be updated when the `ExtensionAPI` interface changes.\n\n```typescript\n/** Full ExtensionAPI reference as markdown with TypeScript code blocks. */\nconst EXTENSION_API_REFERENCE = `# DorkOS Extension API Reference\n\n## ExtensionModule Interface\n\nYour extension must export an \\`activate\\` function:\n\n\\`\\`\\`typescript\nimport type { ExtensionAPI } from '@dorkos/extension-api';\n\nexport function activate(api: ExtensionAPI): void | (() => void) {\n  // Register contributions, return optional cleanup function\n}\n\\`\\`\\`\n\n## ExtensionAPI Interface\n\n\\`\\`\\`typescript\ninterface ExtensionAPI {\n  readonly id: string;\n\n  // --- UI Contributions ---\n  registerComponent(slot: ExtensionPointId, id: string, component: ComponentType, options?: { priority?: number }): () => void;\n  registerCommand(id: string, label: string, callback: () => void, options?: { icon?: string; shortcut?: string }): () => void;\n  registerDialog(id: string, component: ComponentType): { open: () => void; close: () => void };\n  registerSettingsTab(id: string, label: string, component: ComponentType): () => void;\n\n  // --- UI Control ---\n  executeCommand(command: UiCommand): void;\n  openCanvas(content: UiCanvasContent): void;\n  navigate(path: string): void;\n\n  // --- State ---\n  getState(): ExtensionReadableState;\n  subscribe(selector: (state: ExtensionReadableState) => unknown, callback: (value: unknown) => void): () => void;\n\n  // --- Storage ---\n  loadData<T>(): Promise<T | null>;\n  saveData<T>(data: T): Promise<void>;\n\n  // --- Notifications ---\n  notify(message: string, options?: { type?: 'info' | 'success' | 'error' }): void;\n\n  // --- Context ---\n  isSlotAvailable(slot: ExtensionPointId): boolean;\n}\n\\`\\`\\`\n\n## ExtensionPointId (Available Slots)\n\n\\`\\`\\`typescript\ntype ExtensionPointId =\n  | 'sidebar.footer'\n  | 'sidebar.tabs'\n  | 'dashboard.sections'\n  | 'header.actions'\n  | 'command-palette.items'\n  | 'dialog'\n  | 'settings.tabs'\n  | 'session.canvas';\n\\`\\`\\`\n\n## ExtensionReadableState\n\n\\`\\`\\`typescript\ninterface ExtensionReadableState {\n  currentCwd: string | null;\n  activeSessionId: string | null;\n  agentId: string | null;\n}\n\\`\\`\\`\n\n## Usage Examples\n\n### Dashboard Section\n\\`\\`\\`typescript\nexport function activate(api: ExtensionAPI) {\n  api.registerComponent('dashboard.sections', 'my-section', MyComponent, { priority: 50 });\n}\n\\`\\`\\`\n\n### Command Palette Item\n\\`\\`\\`typescript\nexport function activate(api: ExtensionAPI) {\n  api.registerCommand('greet', 'Say Hello', () => {\n    api.notify('Hello from my extension!', { type: 'success' });\n  });\n}\n\\`\\`\\`\n\n### Settings Tab with Persistence\n\\`\\`\\`typescript\nexport function activate(api: ExtensionAPI) {\n  api.registerSettingsTab('config', 'My Settings', MySettingsPanel);\n}\n\\`\\`\\`\n\n### Storage\n\\`\\`\\`typescript\nconst data = await api.loadData<{ count: number }>();\nawait api.saveData({ count: (data?.count ?? 0) + 1 });\n\\`\\`\\`\n`;\n```\n\nAdd the handler factory:\n\n```typescript\n/**\n * Handler factory for `get_extension_api` — returns the full ExtensionAPI\n * type definitions and usage examples as text.\n */\nexport function createGetExtensionApiHandler(_deps: McpToolDeps) {\n  return async () => {\n    return {\n      content: [{ type: 'text' as const, text: EXTENSION_API_REFERENCE }],\n    };\n  };\n}\n```\n\nNote: This handler does NOT guard on `deps.extensionManager` — the API reference is useful even if the extension system is not initialized. The reference is always available.\n\n## File: `apps/server/src/services/core/mcp-server.ts`\n\nAdd to imports and register:\n\n```typescript\nimport {\n  // ... existing imports ...\n  createGetExtensionApiHandler,\n} from '../runtimes/claude-code/mcp-tools/extension-tools.js';\n\nserver.tool(\n  'get_extension_api',\n  'Get the full ExtensionAPI type definitions and usage examples. Call this when writing or debugging an extension to understand the available API surface. Returns TypeScript interface definitions for ExtensionAPI, ExtensionPointId, ExtensionReadableState, and ExtensionModule.',\n  {},\n  createGetExtensionApiHandler(deps)\n);\n```\n\n## Reference Content\n\nThe reference text includes:\n1. `ExtensionModule` contract — the `activate()` function signature and return type\n2. Full `ExtensionAPI` interface — all 13 methods + 1 readonly field\n3. `ExtensionPointId` union — all 8 available slot names\n4. `ExtensionReadableState` interface — the shape returned by `getState()`\n5. Usage examples — dashboard section, command, settings tab, storage\n\nThe text is approximately 200-300 lines / 2000-3000 tokens. Formatted as markdown with TypeScript code blocks for easy agent parsing.\n\n## Maintenance Requirement\n\nWhen the `ExtensionAPI` interface changes (new methods, parameter changes, new slots), the `EXTENSION_API_REFERENCE` constant must be updated to match. A test should verify that the hardcoded reference mentions all method names from the actual interface.\n\n## Acceptance Criteria\n\n- `get_extension_api` tool is registered with no parameters\n- Returns raw text content (not JSON-wrapped) with the full API reference\n- Reference includes all ExtensionAPI methods, all slot IDs, state interface, and usage examples\n- Does not require `extensionManager` to be initialized (always available)\n- A test verifies the reference mentions all method names from the `ExtensionAPI` interface\n- `pnpm typecheck` passes",
      "activeForm": "Implementing get_extension_api MCP tool with full API reference",
      "size": "medium",
      "priority": "medium",
      "dependencies": ["1.2"],
      "parallelWith": ["2.2"]
    },
    {
      "id": "2.2",
      "phase": 2,
      "phaseName": "Agent DX Improvements",
      "subject": "[ext-platform-04-agent-extensions] [P2] Implement testExtension() method on ExtensionManager with MockExtensionAPI",
      "description": "Add the `testExtension()` method to `ExtensionManager` that compiles an extension and activates it against a mock API to verify it loads without errors. Also implement the `MockExtensionAPI` class used for headless testing.\n\n## File: `apps/server/src/services/extensions/extension-manager.ts`\n\nAdd the result type:\n\n```typescript\nimport type { ExtensionPointId, ExtensionReadableState } from '@dorkos/extension-api';\n\n/** Result of headless extension testing. */\nexport interface TestExtensionResult {\n  status: 'ok' | 'error';\n  id: string;\n  phase?: 'compilation' | 'activation';\n  contributions?: Record<ExtensionPointId, number>;\n  errors?: Array<{\n    text: string;\n    location?: { file: string; line: number; column: number };\n  }>;\n  error?: string;\n  stack?: string;\n  message?: string;\n}\n```\n\nAdd the `MockExtensionAPI` class (can be defined as a module-level class in the same file, or in a separate `mock-extension-api.ts`):\n\n```typescript\n/**\n * Lightweight ExtensionAPI stub for headless server-side testing.\n * Implements all methods as no-ops while counting registrations per slot.\n */\nclass MockExtensionAPI {\n  readonly id: string;\n  private counts: Record<string, number> = {};\n\n  constructor(id: string) {\n    this.id = id;\n  }\n\n  registerComponent(slot: string, _id: string, _component: unknown): () => void {\n    this.counts[slot] = (this.counts[slot] ?? 0) + 1;\n    return () => {};\n  }\n\n  registerCommand(_id: string, _label: string, _callback: () => void): () => void {\n    this.counts['command-palette.items'] = (this.counts['command-palette.items'] ?? 0) + 1;\n    return () => {};\n  }\n\n  registerDialog(_id: string, _component: unknown): { open: () => void; close: () => void } {\n    this.counts['dialog'] = (this.counts['dialog'] ?? 0) + 1;\n    return { open: () => {}, close: () => {} };\n  }\n\n  registerSettingsTab(_id: string, _label: string, _component: unknown): () => void {\n    this.counts['settings.tabs'] = (this.counts['settings.tabs'] ?? 0) + 1;\n    return () => {};\n  }\n\n  executeCommand(): void {}\n  openCanvas(): void {}\n  navigate(): void {}\n  getState(): ExtensionReadableState {\n    return { currentCwd: null, activeSessionId: null, agentId: null };\n  }\n  subscribe(): () => void {\n    return () => {};\n  }\n  async loadData(): Promise<null> {\n    return null;\n  }\n  async saveData(): Promise<void> {}\n  notify(): void {}\n  isSlotAvailable(): boolean {\n    return true;\n  }\n\n  /** Return registration counts for all known slots. */\n  getContributions(): Record<string, number> {\n    const allSlots: string[] = [\n      'dashboard.sections',\n      'command-palette.items',\n      'settings.tabs',\n      'sidebar.footer',\n      'sidebar.tabs',\n      'header.actions',\n      'dialog',\n      'session.canvas',\n    ];\n    return Object.fromEntries(allSlots.map((slot) => [slot, this.counts[slot] ?? 0])) as Record<\n      string,\n      number\n    >;\n  }\n}\n```\n\nAdd the method to `ExtensionManager`:\n\n```typescript\n/**\n * Compile an extension and activate it against a mock API to verify\n * it loads without errors. Returns the contribution counts per slot.\n *\n * @param id - Extension identifier\n * @returns Test result with contribution counts or error details\n */\nasync testExtension(id: string): Promise<TestExtensionResult> {\n  const record = this.extensions.get(id);\n  if (!record) {\n    throw new Error(`Extension '${id}' not found`);\n  }\n\n  // Step 1: Compile\n  const compileResult = await this.compiler.compile(record);\n  if ('error' in compileResult) {\n    return {\n      status: 'error',\n      id,\n      phase: 'compilation',\n      errors: compileResult.error.errors,\n    };\n  }\n\n  // Step 2: Read the compiled bundle\n  const bundle = await this.compiler.readBundle(id, compileResult.sourceHash);\n  if (!bundle) {\n    return {\n      status: 'error',\n      id,\n      phase: 'compilation',\n      error: 'Compiled bundle not found in cache',\n    };\n  }\n\n  // Step 3: Evaluate the bundle and extract activate()\n  try {\n    // Create a data URI for dynamic import\n    const dataUri = `data:text/javascript;base64,${Buffer.from(bundle).toString('base64')}`;\n    const module = await import(/* webpackIgnore: true */ dataUri);\n\n    if (typeof module.activate !== 'function') {\n      return {\n        status: 'error',\n        id,\n        phase: 'activation',\n        error: 'Extension does not export an activate() function',\n      };\n    }\n\n    // Step 4: Activate against mock API\n    const mockApi = new MockExtensionAPI(id);\n    module.activate(mockApi);\n\n    const totalContributions = Object.values(mockApi.getContributions()).reduce(\n      (sum, count) => sum + (count as number),\n      0\n    );\n\n    return {\n      status: 'ok',\n      id,\n      contributions: mockApi.getContributions() as Record<ExtensionPointId, number>,\n      message: `Extension activated successfully. Registered ${totalContributions} contribution(s).`,\n    };\n  } catch (err) {\n    return {\n      status: 'error',\n      id,\n      phase: 'activation',\n      error: err instanceof Error ? err.message : String(err),\n      stack: err instanceof Error ? err.stack : undefined,\n    };\n  }\n}\n```\n\n## Server-Side Activation Notes\n\nExtensions are compiled as ESM browser bundles with `react` and `react-dom` externalized. The server-side `testExtension()` evaluates the bundle in a limited Node.js context. React component constructors will not be called — only the `activate()` function runs. `registerComponent` receives an opaque value (the component function) but does not attempt to render it. This is sufficient for verifying that:\n\n1. The extension compiles without TypeScript errors\n2. The `activate()` function runs without throwing\n3. The expected registration methods are called for the expected slots\n\nIf the data URI `import()` approach fails in Node.js (some configurations require `--experimental-vm-modules`), fall back to `new Function()` evaluation or write the bundle to a temp file and import it.\n\n## Acceptance Criteria\n\n- `testExtension(id)` compiles the extension and activates it against `MockExtensionAPI`\n- Returns `{ status: 'ok', contributions: { 'dashboard.sections': 1, ... } }` on success\n- Returns `{ status: 'error', phase: 'compilation', errors: [...] }` on compile failure\n- Returns `{ status: 'error', phase: 'activation', error: '...', stack: '...' }` on runtime failure\n- `MockExtensionAPI` counts registrations per slot correctly\n- `MockExtensionAPI` implements all `ExtensionAPI` methods as no-ops\n- `getContributions()` returns counts for all 8 slot IDs (zero for unused slots)\n- Throws when extension ID is not found\n- `pnpm typecheck` passes",
      "activeForm": "Implementing testExtension() with MockExtensionAPI for headless smoke testing",
      "size": "large",
      "priority": "medium",
      "dependencies": ["1.5"],
      "parallelWith": ["2.1"]
    },
    {
      "id": "2.3",
      "phase": 2,
      "phaseName": "Agent DX Improvements",
      "subject": "[ext-platform-04-agent-extensions] [P2] Implement test_extension MCP tool handler",
      "description": "Add the `test_extension` MCP tool handler to `extension-tools.ts` and register it in `mcp-server.ts`. This tool runs a headless smoke test on an extension.\n\n## File: `apps/server/src/services/runtimes/claude-code/mcp-tools/extension-tools.ts`\n\n```typescript\n/**\n * Handler factory for `test_extension` — headless smoke test that compiles\n * and activates against a mock API, returning contribution counts.\n */\nexport function createTestExtensionHandler(deps: McpToolDeps) {\n  return async (args: { id: string }) => {\n    const manager = requireExtensionManager(deps);\n    if (!manager) {\n      return jsonContent({ error: 'Extension system is not available' }, true);\n    }\n\n    try {\n      const result = await manager.testExtension(args.id);\n      return jsonContent(result);\n    } catch (err) {\n      return jsonContent(\n        { error: err instanceof Error ? err.message : 'Failed to test extension' },\n        true\n      );\n    }\n  };\n}\n```\n\n## File: `apps/server/src/services/core/mcp-server.ts`\n\nAdd to imports and register:\n\n```typescript\nimport {\n  // ... existing imports ...\n  createTestExtensionHandler,\n} from '../runtimes/claude-code/mcp-tools/extension-tools.js';\n\nserver.tool(\n  'test_extension',\n  'Run a headless smoke test on an extension. Compiles the extension (catching TypeScript errors) and activates it against a mock ExtensionAPI (catching runtime errors). Returns the contributions map showing what the extension registered in each slot. Does not render UI — use reload_extensions for visual verification.',\n  {\n    id: z.string().describe('Extension ID to test'),\n  },\n  createTestExtensionHandler(deps)\n);\n```\n\n## Return Shape (success)\n\n```json\n{\n  \"status\": \"ok\",\n  \"id\": \"github-prs\",\n  \"contributions\": {\n    \"dashboard.sections\": 1,\n    \"command-palette.items\": 0,\n    \"settings.tabs\": 0,\n    \"sidebar.footer\": 0,\n    \"sidebar.tabs\": 0,\n    \"header.actions\": 0,\n    \"dialog\": 0,\n    \"session.canvas\": 0\n  },\n  \"message\": \"Extension activated successfully. Registered 1 contribution(s).\"\n}\n```\n\n## Return Shape (compilation error)\n\n```json\n{\n  \"status\": \"error\",\n  \"id\": \"github-prs\",\n  \"phase\": \"compilation\",\n  \"errors\": [\n    { \"text\": \"Property 'registerWidget' does not exist on type 'ExtensionAPI'\", \"location\": { \"file\": \"index.ts\", \"line\": 15, \"column\": 8 } }\n  ]\n}\n```\n\n## Return Shape (activation error)\n\n```json\n{\n  \"status\": \"error\",\n  \"id\": \"github-prs\",\n  \"phase\": \"activation\",\n  \"error\": \"TypeError: Cannot read properties of undefined (reading 'map')\",\n  \"stack\": \"TypeError: Cannot read properties of undefined...\\n    at activate (index.ts:12:5)\"\n}\n```\n\n## Agent Workflow\n\nThe `test_extension` tool fits into the agent iteration loop:\n\n1. `create_extension` — scaffold the extension\n2. `test_extension` — headless smoke test (catches ~80% of failures without browser)\n3. `reload_extensions` — visual verification in the browser\n4. Iterate: edit source, `test_extension` or `reload_extensions`, repeat\n\n## Acceptance Criteria\n\n- `test_extension` tool is registered with required `id` parameter\n- Returns `testExtension()` result directly as JSON\n- Unknown extension ID returns `isError: true`\n- Extension system not available returns `isError: true`\n- `pnpm typecheck` passes",
      "activeForm": "Implementing test_extension MCP tool handler",
      "size": "small",
      "priority": "medium",
      "dependencies": ["2.2"],
      "parallelWith": []
    },
    {
      "id": "2.4",
      "phase": 2,
      "phaseName": "Agent DX Improvements",
      "subject": "[ext-platform-04-agent-extensions] [P2] Add SSE extension_reloaded event broadcast from server",
      "description": "Add a mechanism for the server to broadcast `extension_reloaded` SSE events to connected clients when extensions are recompiled. This enables the client to perform per-extension hot reload without polling.\n\n## Approach\n\nAdd a lightweight global SSE endpoint at `GET /api/extensions/events` that clients subscribe to for extension lifecycle events. This is separate from the session-scoped SSE connections.\n\n## File: `apps/server/src/routes/extensions.ts`\n\nAdd a new SSE endpoint and a broadcast function:\n\n```typescript\nimport type { Response } from 'express';\n\n/** Connected SSE clients for extension events. */\nconst sseClients = new Set<Response>();\n\n/**\n * Broadcast an extension_reloaded event to all connected SSE clients.\n *\n * @param extensionIds - IDs of the extensions that were reloaded\n */\nexport function broadcastExtensionReloaded(extensionIds: string[]): void {\n  const data = JSON.stringify({\n    extensions: extensionIds,\n    timestamp: new Date().toISOString(),\n  });\n\n  for (const client of sseClients) {\n    try {\n      client.write(`event: extension_reloaded\\ndata: ${data}\\n\\n`);\n    } catch {\n      // Client disconnected — will be removed on close event\n      sseClients.delete(client);\n    }\n  }\n}\n```\n\nInside `createExtensionsRouter()`, add the SSE endpoint:\n\n```typescript\n// GET /api/extensions/events — SSE stream for extension lifecycle events\nrouter.get('/events', (_req, res) => {\n  res.set({\n    'Content-Type': 'text/event-stream',\n    'Cache-Control': 'no-cache',\n    Connection: 'keep-alive',\n  });\n  res.flushHeaders();\n\n  sseClients.add(res);\n\n  // Send initial heartbeat\n  res.write(':ok\\n\\n');\n\n  // Clean up on disconnect\n  res.on('close', () => {\n    sseClients.delete(res);\n  });\n});\n```\n\n## File: `apps/server/src/services/runtimes/claude-code/mcp-tools/extension-tools.ts`\n\nUpdate `createReloadExtensionsHandler` and `createCreateExtensionHandler` to broadcast the SSE event after successful operations:\n\n```typescript\nimport { broadcastExtensionReloaded } from '../../../../routes/extensions.js';\n\n// In createReloadExtensionsHandler, after successful reload:\nif (args.id) {\n  const result = await manager.reloadExtension(args.id);\n  if (result.status === 'compiled') {\n    broadcastExtensionReloaded([args.id]);\n  }\n  // ... return result\n} else {\n  const extensions = await manager.reload();\n  const compiledIds = extensions\n    .filter((ext) => ext.status === 'compiled')\n    .map((ext) => ext.id);\n  if (compiledIds.length > 0) {\n    broadcastExtensionReloaded(compiledIds);\n  }\n  // ... return results\n}\n\n// In createCreateExtensionHandler, after successful creation:\nif (result.bundleReady) {\n  broadcastExtensionReloaded([result.id]);\n}\n```\n\n## SSE Event Format\n\n```\nevent: extension_reloaded\ndata: {\"extensions\":[\"github-prs\"],\"timestamp\":\"2026-03-26T14:30:00.000Z\"}\n```\n\n## Event Payload Type\n\n```typescript\ninterface ExtensionReloadedEvent {\n  /** Extension IDs that were reloaded. */\n  extensions: string[];\n  /** ISO timestamp of the reload. */\n  timestamp: string;\n}\n```\n\nBroadcast only happens for extensions that compiled successfully. If all extensions fail compilation, no SSE event is sent (the client has nothing to reload).\n\n## Acceptance Criteria\n\n- `GET /api/extensions/events` returns an SSE stream\n- `extension_reloaded` event is sent when `reload_extensions` completes with at least one successful compilation\n- `extension_reloaded` event is sent when `create_extension` completes with successful compilation\n- Event payload includes the array of reloaded extension IDs and a timestamp\n- Disconnected clients are cleaned up automatically\n- No event sent when all extensions fail compilation\n- `pnpm typecheck` passes",
      "activeForm": "Adding SSE extension_reloaded event broadcast from server",
      "size": "medium",
      "priority": "medium",
      "dependencies": ["1.6"],
      "parallelWith": ["2.1", "2.2"]
    },
    {
      "id": "2.5",
      "phase": 2,
      "phaseName": "Agent DX Improvements",
      "subject": "[ext-platform-04-agent-extensions] [P2] Client-side per-extension hot reload via SSE event",
      "description": "Update the client-side extension system to subscribe to the `extension_reloaded` SSE event and perform per-extension hot reload: deactivate the changed extension, re-import its bundle with a cache-busted URL, and reactivate it.\n\n## File: `apps/client/src/layers/features/extensions/model/extension-loader.ts`\n\nAdd a new method to `ExtensionLoader` for targeted hot reload:\n\n```typescript\n/**\n * Hot-reload specific extensions: deactivate, re-import, and reactivate.\n * Extensions not in the provided list are untouched — their state and\n * registrations are preserved.\n *\n * @param ids - Extension IDs to reload\n * @returns Updated loaded map and refreshed extension list\n */\nasync reloadExtensions(ids: string[]): Promise<{\n  extensions: ExtensionRecordPublic[];\n  loaded: Map<string, LoadedExtension>;\n}> {\n  // 1. Deactivate the specified extensions\n  for (const id of ids) {\n    const ext = this.loaded.get(id);\n    if (ext) {\n      try {\n        ext.deactivate?.();\n      } catch (err) {\n        console.error(`[extensions] Error deactivating ${id}:`, err);\n      }\n      for (const cleanup of ext.cleanups) {\n        try {\n          cleanup();\n        } catch (err) {\n          console.error(`[extensions] Error in cleanup for ${id}:`, err);\n        }\n      }\n      this.loaded.delete(id);\n    }\n  }\n\n  // 2. Fetch updated extension list from server\n  const extensions = await fetchExtensions();\n\n  // 3. Re-import and reactivate the specified extensions\n  for (const id of ids) {\n    const rec = extensions.find((e) => e.id === id);\n    if (!rec || rec.status !== 'compiled' || !rec.bundleReady) {\n      continue;\n    }\n\n    try {\n      // Cache-bust: append timestamp to force fresh module evaluation\n      const module = (await import(\n        /* @vite-ignore */ `/api/extensions/${id}/bundle?t=${Date.now()}`\n      )) as ExtensionModule;\n\n      const { api, cleanups } = createExtensionAPI(id, this.deps);\n      const deactivateFn = module.activate(api);\n\n      this.loaded.set(id, {\n        id,\n        manifest: rec.manifest,\n        module,\n        api,\n        cleanups,\n        deactivate: typeof deactivateFn === 'function' ? deactivateFn : undefined,\n      });\n\n      console.log(`[extensions] Hot-reloaded: ${rec.manifest.name} v${rec.manifest.version}`);\n    } catch (err) {\n      console.error(`[extensions] Failed to hot-reload ${id}:`, err);\n    }\n  }\n\n  return { extensions, loaded: this.loaded };\n}\n```\n\nNote: Make the `fetchExtensions()` function accessible to the class (either import it or move it to be a method or a shared module-level function). It is currently a module-level function in the same file, so it is already accessible.\n\n## File: `apps/client/src/layers/features/extensions/model/extension-context.tsx`\n\nAdd an `useEffect` that creates an `EventSource` connection to subscribe to `extension_reloaded` events:\n\n```typescript\nexport function ExtensionProvider({ deps, children }: ExtensionProviderProps) {\n  const [state, setState] = useState<ExtensionContextValue>(defaultContextValue);\n  const loaderRef = useRef<ExtensionLoader | null>(null);\n\n  useCwdExtensionSync();\n\n  // Initial load\n  useEffect(() => {\n    const loader = new ExtensionLoader(deps);\n    loaderRef.current = loader;\n\n    loader\n      .initialize()\n      .then(({ extensions, loaded }) => {\n        setState({ extensions, loaded, ready: true });\n      })\n      .catch((err: unknown) => {\n        console.error('[extensions] Failed to initialize:', err);\n        setState((prev) => ({ ...prev, ready: true }));\n      });\n\n    return () => {\n      loader.deactivateAll();\n      loaderRef.current = null;\n    };\n    // eslint-disable-next-line react-hooks/exhaustive-deps\n  }, []);\n\n  // SSE subscription for hot reload\n  useEffect(() => {\n    const eventSource = new EventSource('/api/extensions/events');\n\n    eventSource.addEventListener('extension_reloaded', async (event) => {\n      const loader = loaderRef.current;\n      if (!loader) return;\n\n      try {\n        const data = JSON.parse(event.data) as { extensions: string[]; timestamp: string };\n        const { extensions, loaded } = await loader.reloadExtensions(data.extensions);\n        setState({ extensions, loaded, ready: true });\n      } catch (err) {\n        console.error('[extensions] Hot reload failed:', err);\n      }\n    });\n\n    return () => {\n      eventSource.close();\n    };\n  }, []);\n\n  return <ExtensionContext.Provider value={state}>{children}</ExtensionContext.Provider>;\n}\n```\n\nKey changes from the current implementation:\n1. Store the `ExtensionLoader` in a `useRef` so the SSE handler can access it\n2. Add a second `useEffect` that creates an `EventSource` connection to `/api/extensions/events`\n3. On `extension_reloaded` event, call `loader.reloadExtensions(ids)` and update React state\n4. The `EventSource` is cleaned up on unmount\n\n## Bundle URL Cache Busting\n\nThe browser caches ESM modules by URL. When an extension is recompiled, the same URL (`/api/extensions/{id}/bundle`) would return the cached module from the browser's ESM module cache (which is separate from the HTTP cache, even though the server sets `Cache-Control: no-store`). Appending `?t=${Date.now()}` forces a fresh module evaluation.\n\nMemory note: Each `import()` with a unique URL creates a new module instance that cannot be garbage collected. For interactive development loops (10-50 reloads), this is acceptable (~50-500KB per module instance). A page reload clears all module instances.\n\n## Import Changes\n\nAdd `useRef` to the React imports in `extension-context.tsx`:\n\n```typescript\nimport { createContext, useContext, useEffect, useRef, useState, type ReactNode } from 'react';\n```\n\n## Acceptance Criteria\n\n- `ExtensionLoader.reloadExtensions(ids)` deactivates, re-imports, and reactivates only the specified extensions\n- Other loaded extensions are not affected by a reload (state preserved)\n- `ExtensionProvider` subscribes to `GET /api/extensions/events` via `EventSource`\n- On `extension_reloaded` event, performs hot reload and updates React state\n- Bundle URLs use cache-busting query parameters (`?t=...`)\n- `EventSource` is properly cleaned up on unmount\n- `pnpm typecheck` passes",
      "activeForm": "Implementing client-side per-extension hot reload via SSE events",
      "size": "large",
      "priority": "medium",
      "dependencies": ["2.4"],
      "parallelWith": []
    },
    {
      "id": "2.6",
      "phase": 2,
      "phaseName": "Agent DX Improvements",
      "subject": "[ext-platform-04-agent-extensions] [P2] Unit tests for Phase 2 tools and client hot reload",
      "description": "Write unit tests for the Phase 2 MCP tool handlers (`get_extension_api`, `test_extension`), the `testExtension()` method, `MockExtensionAPI`, and the client-side hot reload logic.\n\n## File: `apps/server/src/services/runtimes/claude-code/mcp-tools/__tests__/extension-tools.test.ts`\n\nAdd test scenarios to the existing test file:\n\n### Test Scenarios for `get_extension_api`\n\n1. **Returns text content (not JSON)**: Verify the response has `content` array with a single `{ type: 'text', text: '...' }` entry (not wrapped in JSON via `jsonContent()`).\n2. **Reference contains all ExtensionAPI methods**: Parse the returned text and verify it mentions all method names: `registerComponent`, `registerCommand`, `registerDialog`, `registerSettingsTab`, `executeCommand`, `openCanvas`, `navigate`, `getState`, `subscribe`, `loadData`, `saveData`, `notify`, `isSlotAvailable`.\n3. **Reference contains all slot IDs**: Verify the text includes all 8 slot IDs: `sidebar.footer`, `sidebar.tabs`, `dashboard.sections`, `header.actions`, `command-palette.items`, `dialog`, `settings.tabs`, `session.canvas`.\n4. **Does not require extensionManager**: Create deps without `extensionManager`. Verify the handler still returns the reference (no error).\n5. **Reference contains usage examples**: Verify the text includes code blocks with `api.registerComponent`, `api.registerCommand`, `api.loadData`.\n\n### Test Scenarios for `test_extension`\n\n1. **Returns success with contribution counts**: Mock `testExtension()` returning `{ status: 'ok', id: 'test', contributions: { 'dashboard.sections': 1, ... } }`. Verify response matches.\n2. **Returns compilation error**: Mock `testExtension()` returning `{ status: 'error', phase: 'compilation', errors: [...] }`. Verify response matches.\n3. **Returns activation error**: Mock `testExtension()` returning `{ status: 'error', phase: 'activation', error: '...', stack: '...' }`. Verify response matches.\n4. **Returns not-found error**: Mock `testExtension()` throwing `\"Extension 'x' not found\"`. Verify `isError: true`.\n5. **Returns error when manager missing**: Verify `isError: true`.\n\n## File: `apps/server/src/services/extensions/__tests__/extension-manager-test.test.ts` (NEW)\n\nTest `testExtension()` and `MockExtensionAPI`:\n\n### MockExtensionAPI Tests\n\n1. **Counts registerComponent calls per slot**: Call `registerComponent('dashboard.sections', ...)` twice. Verify `getContributions()` returns `{ 'dashboard.sections': 2, ... }` with all other slots at 0.\n2. **Counts registerCommand calls**: Call `registerCommand(...)` once. Verify `getContributions()` returns `{ 'command-palette.items': 1, ... }`.\n3. **Counts registerDialog calls**: Call `registerDialog(...)` once. Verify `{ 'dialog': 1, ... }`.\n4. **Counts registerSettingsTab calls**: Call `registerSettingsTab(...)` once. Verify `{ 'settings.tabs': 1, ... }`.\n5. **Returns unsubscribe functions**: Verify `registerComponent()`, `registerCommand()` return functions. Verify `registerDialog()` returns `{ open, close }` with callable functions.\n6. **All no-op methods don't throw**: Call `executeCommand()`, `openCanvas()`, `navigate()`, `getState()`, `subscribe()`, `loadData()`, `saveData()`, `notify()`, `isSlotAvailable()`. None should throw.\n7. **getContributions returns all 8 slots**: Even with no calls, verify all 8 slots are present with count 0.\n\n### testExtension() Tests\n\nThese tests require mocking the `ExtensionCompiler` since `testExtension()` uses `this.compiler.compile()` and `this.compiler.readBundle()`:\n\n1. **Returns ok with contributions for valid extension**: Mock successful compile and a bundle that exports `activate()` calling `registerComponent('dashboard.sections', ...)`. Verify `{ status: 'ok', contributions: { 'dashboard.sections': 1 } }`.\n2. **Returns compilation error for broken extension**: Mock compile returning error. Verify `{ status: 'error', phase: 'compilation', errors: [...] }`.\n3. **Returns activation error when activate() throws**: Mock successful compile, but bundle's `activate()` throws. Verify `{ status: 'error', phase: 'activation', error: '...', stack: '...' }`.\n4. **Throws for unknown extension ID**: Verify error message includes the ID.\n\n## File: `apps/client/src/layers/features/extensions/model/__tests__/extension-loader-reload.test.ts` (NEW)\n\nTest `ExtensionLoader.reloadExtensions()`:\n\n```typescript\n/**\n * @vitest-environment jsdom\n */\nimport { describe, it, expect, vi, beforeEach } from 'vitest';\n```\n\n### Test Scenarios\n\n1. **Deactivates specified extensions**: Load extension 'a', then call `reloadExtensions(['a'])`. Verify the deactivate function was called.\n2. **Preserves unspecified extensions**: Load extensions 'a' and 'b'. Call `reloadExtensions(['a'])`. Verify 'b' is still in the loaded map and its deactivate was NOT called.\n3. **Re-imports with cache-bust URL**: Mock the dynamic `import()`. Verify the URL includes a `?t=` query parameter.\n4. **Runs all cleanup functions**: Load extension with 3 cleanup functions. Reload it. Verify all 3 cleanups were called.\n5. **Handles deactivate errors gracefully**: Load extension whose deactivate throws. Reload it. Verify no unhandled error and the extension is still reloaded.\n\n## Acceptance Criteria\n\n- All test files created and passing\n- `get_extension_api` tests verify all method names and slot IDs are present in the reference\n- `MockExtensionAPI` tests verify counting logic for all 4 registration methods\n- `testExtension()` tests cover success, compilation error, and activation error paths\n- Client hot reload tests verify deactivation, preservation, and cache busting\n- `pnpm vitest run` passes for all test files",
      "activeForm": "Writing unit tests for Phase 2 tools and client hot reload",
      "size": "large",
      "priority": "medium",
      "dependencies": ["2.1", "2.2", "2.3", "2.5"],
      "parallelWith": []
    },
    {
      "id": "3.1",
      "phase": 3,
      "phaseName": "Integration & Documentation",
      "subject": "[ext-platform-04-agent-extensions] [P3] End-to-end integration test for extension lifecycle",
      "description": "Create an integration test that verifies the full extension lifecycle: create an extension via MCP tools, reload it, verify it appears in the extension list, and confirm the rendering path works.\n\n## File: `apps/server/src/services/core/__tests__/mcp-extension-tools.test.ts` (NEW)\n\nThis test creates a real `ExtensionManager` (with a temp directory for `dorkHome`) and verifies the MCP tools work end-to-end through the handler layer.\n\n```typescript\nimport { describe, it, expect, beforeAll, afterAll } from 'vitest';\nimport fs from 'fs/promises';\nimport os from 'os';\nimport path from 'path';\nimport {\n  createListExtensionsHandler,\n  createCreateExtensionHandler,\n  createReloadExtensionsHandler,\n  createGetExtensionErrorsHandler,\n  createGetExtensionApiHandler,\n  createTestExtensionHandler,\n} from '../../runtimes/claude-code/mcp-tools/extension-tools.js';\nimport { ExtensionManager } from '../../extensions/extension-manager.js';\nimport type { McpToolDeps } from '../../runtimes/claude-code/mcp-tools/types.js';\n\ndescribe('MCP Extension Tools — Integration', () => {\n  let tempDir: string;\n  let dorkHome: string;\n  let cwd: string;\n  let manager: ExtensionManager;\n  let deps: McpToolDeps;\n\n  beforeAll(async () => {\n    // Create temp directory structure\n    tempDir = await fs.mkdtemp(path.join(os.tmpdir(), 'dorkos-ext-test-'));\n    dorkHome = path.join(tempDir, '.dork');\n    cwd = path.join(tempDir, 'project');\n    await fs.mkdir(cwd, { recursive: true });\n\n    // Initialize extension manager\n    manager = new ExtensionManager(dorkHome);\n    await manager.initialize(cwd);\n\n    deps = {\n      transcriptReader: {} as McpToolDeps['transcriptReader'],\n      defaultCwd: cwd,\n      extensionManager: manager,\n    };\n  });\n\n  afterAll(async () => {\n    await fs.rm(tempDir, { recursive: true, force: true });\n  });\n\n  // Test scenarios:\n});\n```\n\n### Test Scenarios\n\n1. **All 6 tools are callable**: Create all 6 handlers. Call each with valid/minimal parameters. Verify none throw unhandled errors.\n\n2. **create_extension produces working extension**: Call `createCreateExtensionHandler(deps)` with `{ name: 'test-card', template: 'dashboard-card', scope: 'local' }`. Verify:\n   - Response has `created: true`\n   - `status` is `'compiled'`\n   - `bundleReady` is `true`\n   - `extension.json` exists on disk at `{cwd}/.dork/extensions/test-card/extension.json`\n   - `index.ts` exists on disk at `{cwd}/.dork/extensions/test-card/index.ts`\n\n3. **list_extensions includes created extension**: After creating, call `createListExtensionsHandler(deps)`. Verify the response `extensions` array includes an entry with `id: 'test-card'` and `status: 'compiled'`.\n\n4. **reload_extensions recompiles after edit**: Write a modified `index.ts` to the extension directory (change a string literal). Call `createReloadExtensionsHandler(deps)` with `{ id: 'test-card' }`. Verify `status: 'compiled'`.\n\n5. **get_extension_errors returns no errors for healthy extension**: Call `createGetExtensionErrorsHandler(deps)` with `{ id: 'test-card' }`. Verify `error: null`.\n\n6. **reload_extensions returns structured errors for broken code**: Write invalid TypeScript to `index.ts` (e.g., `export const x: number = 'not a number';`). Call reload. Verify the response includes `status: 'compile_error'` with an error object containing `errors` array with at least one entry that has `text` and `location`.\n\n7. **get_extension_api returns complete reference**: Call `createGetExtensionApiHandler(deps)`. Verify the response text contains `registerComponent`, `ExtensionPointId`, and `dashboard.sections`.\n\n8. **test_extension returns contribution counts**: Call `createTestExtensionHandler(deps)` with the test-card extension (after restoring valid code). Verify `status: 'ok'` and `contributions['dashboard.sections'] >= 1`.\n\n9. **create_extension with command template**: Call create with `template: 'command'`. Verify response has `template: 'command'` and the manifest on disk has `contributions: { 'command-palette.items': true }`.\n\n10. **create_extension with settings-panel template**: Call create with `template: 'settings-panel'`. Verify the manifest on disk has `contributions: { 'settings.tabs': true }`.\n\n11. **create_extension rejects duplicate**: Call create with the same name twice. Second call should return `isError: true` with \"already exists\" message.\n\n## Performance Assertion\n\nFor the reload test (scenario 4), measure the time from calling reload to receiving the result. Assert it completes in under 2000ms (generous bound for CI; typical is <300ms).\n\n## Acceptance Criteria\n\n- Integration test creates extensions on a real temp filesystem\n- Uses real `ExtensionManager` and `ExtensionCompiler` (not mocked)\n- All 11 scenarios pass\n- Clean up temp directory after tests\n- Test completes in under 30 seconds total\n- `pnpm vitest run` passes",
      "activeForm": "Creating end-to-end integration test for extension lifecycle",
      "size": "large",
      "priority": "medium",
      "dependencies": ["1.8", "2.6"],
      "parallelWith": ["3.2"]
    },
    {
      "id": "3.2",
      "phase": 3,
      "phaseName": "Integration & Documentation",
      "subject": "[ext-platform-04-agent-extensions] [P3] Update contributing docs with agent extension workflow",
      "description": "Update the existing contributing guides to document the new agent extension workflow, MCP tools, and ExtensionAPI reference.\n\n## File: `contributing/architecture.md`\n\nAdd a new subsection under the MCP server section:\n\n```markdown\n### Extension MCP Tools\n\nThe external MCP server registers 6 extension management tools:\n\n| Tool | Description |\n|------|-------------|\n| `list_extensions` | List all extensions with status and scope |\n| `create_extension` | Scaffold a new extension with template code |\n| `reload_extensions` | Recompile and reload (all or single) |\n| `get_extension_errors` | Get detailed errors for a specific extension |\n| `get_extension_api` | Get full ExtensionAPI type reference |\n| `test_extension` | Headless smoke test (compile + mock activate) |\n\nTools are implemented in `apps/server/src/services/runtimes/claude-code/mcp-tools/extension-tools.ts` and registered in `mcp-server.ts`. All handlers guard on `deps.extensionManager` — when extensions are disabled, tools return descriptive errors.\n\nThe agent iteration loop: `create_extension` → `test_extension` (smoke) → `reload_extensions` (visual) → iterate.\n```\n\n## File: `contributing/api-reference.md`\n\nAdd a new section documenting the 6 MCP tools with their parameter schemas:\n\n```markdown\n## Extension MCP Tools\n\n### list_extensions\n- **Parameters**: None\n- **Returns**: `{ extensions: ExtensionInfo[], count: number }`\n\n### create_extension\n- **Parameters**:\n  - `name` (string, required): Extension ID (kebab-case)\n  - `description` (string, optional): Short description\n  - `template` (enum, optional): `dashboard-card` | `command` | `settings-panel` (default: `dashboard-card`)\n  - `scope` (enum, optional): `global` | `local` (default: `local`)\n- **Returns**: `{ created: true, id, path, scope, template, status, bundleReady, files, message }`\n\n### reload_extensions\n- **Parameters**:\n  - `id` (string, optional): Extension ID for targeted reload\n- **Returns**: `{ results: Record<string, ReloadResult>, summary: { total, compiled, errors } }`\n\n### get_extension_errors\n- **Parameters**:\n  - `id` (string, required): Extension ID\n- **Returns**: `{ id, status, error: ErrorObject | null }`\n\n### get_extension_api\n- **Parameters**: None\n- **Returns**: Full ExtensionAPI type definitions and usage examples as markdown text\n\n### test_extension\n- **Parameters**:\n  - `id` (string, required): Extension ID\n- **Returns**: `{ status: 'ok' | 'error', id, contributions?, phase?, errors?, error?, stack? }`\n```\n\nInclude the SSE event documentation:\n\n```markdown\n### SSE Extension Events\n\n**Endpoint**: `GET /api/extensions/events`\n\n**Event**: `extension_reloaded`\n```json\n{\"extensions\": [\"ext-id\"], \"timestamp\": \"2026-03-26T14:30:00.000Z\"}\n```\n\nSent when extensions are recompiled (via `reload_extensions` or `create_extension`).\n```\n\n## Acceptance Criteria\n\n- `contributing/architecture.md` includes Extension MCP Tools section with the tool table and iteration loop\n- `contributing/api-reference.md` includes all 6 MCP tools with parameter schemas, return types, and SSE event documentation\n- No new files created — only existing guides updated\n- Documentation is concise and follows existing style\n- `pnpm format` passes (no formatting issues in markdown)",
      "activeForm": "Updating contributing docs with agent extension workflow documentation",
      "size": "small",
      "priority": "low",
      "dependencies": ["2.6"],
      "parallelWith": ["3.1"]
    }
  ]
}

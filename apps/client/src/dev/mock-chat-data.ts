/**
 * Mock chat data for playground demos and showcases.
 *
 * Split into three modules by concern:
 * - `mock-factories` — factory functions for creating test data
 * - `mock-tool-calls` — pre-built tool call variant sets
 * - `mock-samples` — sample messages, tasks, files, and questions
 */
export * from './mock-factories';
export * from './mock-tool-calls';
export * from './mock-samples';

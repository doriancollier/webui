/**
 * Discovery services — filesystem scanning for AI-configured projects.
 *
 * The standalone scanner has been replaced by the unified scanner in
 * `@dorkos/mesh`. See `packages/mesh/src/discovery/` for the implementation.
 *
 * @module services/discovery
 */
